{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Compatibility preset for in-range Composer lockfile updates.",
  "packageRules": [
    {
      "description": "Allow only in-range Composer updates at the historical priority.",
      "matchManagers": ["composer"],
      "rangeStrategy": "in-range-only",
      "prPriority": -666
    }
  ]
}
