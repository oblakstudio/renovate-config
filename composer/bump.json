{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Compatibility preset for Composer bump range behavior.",
  "packageRules": [
    {
      "description": "Bump Composer constraints and retain the historical priority.",
      "matchManagers": ["composer"],
      "rangeStrategy": "bump",
      "prPriority": 13
    }
  ]
}
